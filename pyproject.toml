[project]
name = "osintinator"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings",
    "httpx",
    "jinja2",
    "phonenumbers",
    "email-validator",
    "typer[all]",
    "rich",
]

[project.scripts]
osintinator = "cli:app"
